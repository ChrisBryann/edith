services:
  # Backend
  api:
    build: .
    container_name: edith_api
    command: ["python", "-u", "edith/api.py"]
    volumes:
      - .:/app
      - /app/.venv
      - api_hf_models:/app/.cache/huggingface/hub
    ports:
      - "8000:8000"
      - "8080:8080"
    env_file:
      - .env
    stdin_open: true
    tty: true

  # Frontend
  frontend:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules # Isolated volume for container-specific dependencies
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host"
    environment:
      - VITE_API_URL=http://localhost:8000
    depends_on:
<<<<<<< Updated upstream
      - api
=======
      - api

  next-frontend:
    build:
      context: ./next-frontend
      dockerfile: Dockerfile
      target: dev
    env_file: ./next-frontend/.env
    volumes:
      - ./next-frontend:/app
      - next_node_modules:/app/node_modules
      - next_cache:/app/.next
    ports:
      - 3000:3000
    depends_on:
      - api
  
  redis:
    image: redis:8.2.4-alpine3.22
    restart: always
    command: redis-server --save 20 1 --loglevel warning --requirepass admin1234
    volumes:
      - redis_cache:/data

  
volumes:
  api_hf_models:
  frontend_node_modules:
  next_node_modules:
  next_cache:
  redis_cache:
    
>>>>>>> Stashed changes
