[pytest]
markers =
    integration: marks tests as integration tests (deselect with '-m "not integration"')
    offline: marks tests that do not require external connectivity
testpaths = tests
python_files = test_*.py